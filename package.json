{
  "name": "campus-intelligence-root",
  "version": "1.0.0",
  "description": "Unified Campus Intelligence Dashboard monorepo",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix mcp-servers/library && npm install --prefix mcp-servers/cafeteria && npm install --prefix mcp-servers/events && npm install --prefix mcp-servers/academics && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
